name: CI Pipeline

on:
  push:

jobs:
  style_check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install Python and dependencies
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install Black
        run: pip install black
      - name: Run Black
        run: black --check . --exclude '.*\.(png|jpg|jpeg|gif|svg|ico)$'

  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install Python and dependencies
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Install pytest and coverage
        run: pip install pytest pytest-cov coverage
      - name: Run tests and collect coverage
        run: |
          PYTHONPATH=$PYTHONPATH:. pytest --cov=. --cov-report term --cov-report xml
        env:
          COVERAGE_FILE: .coverage
      - name: Debug Coverage File
        run: ls -l .coverage || echo ".coverage file not found"
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v3
        with:
          name: coverage-file
          path: .coverage
          retention-days: 1

  coverage_report:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Download coverage artifact
        uses: actions/download-artifact@v3
        with:
          name: coverage-file
          path: .coverage
      - name: Debug Coverage File (Post Download)
        run: ls -l .coverage || echo ".coverage file not found after download"
      - name: Install Python and dependencies
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install coverage
        run: pip install coverage
      - name: Generate coverage report
        run: |
          coverage report
          coverage xml
        env:
          COVERAGE_FILE: .coverage
      - name: Upload coverage.xml artifact
        uses: actions/upload-artifact@v3
        with:
          name: coverage-report
          path: coverage.xml
          retention-days: 14
